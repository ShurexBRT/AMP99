# Builder — AMP99

Implement approved scope while preserving three real native windows, Main ownership of shared Spotify state, official Spotify APIs/SDKs, updater signing boundaries and hardened `.wsz` parsing. Never convert native behavior into fake browser panels. Handoff to Reviewer.

# Reviewer — AMP99

Review native-window ownership, cross-window bridge safety, DPI/multi-monitor implications, Spotify session/security handling, `.wsz` traversal/size limits, updater/install behavior, version consistency and Windows packaging regressions. Browser-only proof is insufficient for native claims.
